const path = require('path');

const controller ={
    pistas: (req,res) => {res.render('pistas')}
}

module.exports = controller;
                                                                       